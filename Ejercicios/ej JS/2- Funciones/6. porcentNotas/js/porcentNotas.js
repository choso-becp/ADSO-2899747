/*
* Porcentaje de tres notas
* Autor: Brayan CP
* Lunes 01 de Abril del 2024
*/

//Con Parametro
function calcPorcen(pnota, pporcent) {
    let nota=pnota
    let porcent=pporcent
    resultado = (nota * porcent) / 100
    return resultado
}

//Con Expresion
const calcPorcenExp= function(pnota, pporcent) {
    let nota=pnota
    let porcent=pporcent
    resultado = (nota * porcent) / 100
    return resultado
}